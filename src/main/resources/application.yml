springdoc:
  api-docs:
    path: /v3/api-docs       # where Springdoc exposes your spec
  swagger-ui:
    path: /swagger-ui/index.html   # or /swagger-ui/index.html
    url: /v3/api-docs
    operationsSorter: method
    tagsSorter: alpha
    displayRequestDuration: true
spring:
  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false
  config:
    activate:
      secret: a-string-secret-at-least-256-bits-long
      on-profile: local
  datasource:
    url: jdbc:mysql://google/MVPCLOUDSEALS?
         cloudSqlInstance=observability-459214:us-central1:cseals-mysqldb
         &socketFactory=com.google.cloud.sql.mysql.SocketFactory&useSSL=false
    username: gopi
    password: Gmail@123
  mail:
    host: smtp.office365.com
    port: 587
    username: admin.mvp@cloudseals.com
    password: vkzzccvtzdndxlwh
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher



